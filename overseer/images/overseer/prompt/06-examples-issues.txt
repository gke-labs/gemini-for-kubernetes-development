Creating a task to fix an issue:
```bash
factory fix --url $REPO_URL/issues/<issue-number> --tmux --watch --watch-timeout 10h --cleanup
```

Cleaning up lingering sandboxes:
```bash
factory cleanup
```

