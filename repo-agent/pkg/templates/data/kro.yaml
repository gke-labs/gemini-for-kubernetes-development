apiVersion: review.gemini.google.com/v1alpha1
kind: RepoWatch
metadata:
  name: kro
spec:
  repoURL: https://github.com/kubernetes-sigs/kro
  pollIntervalSeconds: 300
  githubSecretName: github-pat
  dev:
    maxActiveSandboxes: 0
    maxSandboxes: 0
    image: ghcr.io/gke-labs/gemini-for-kubernetes-development/generic-golang:latest
    llm:
      apiKeySecretRef: gemini-vscode-tokens
  review:
    reviewShutdownAfterMinutes: 30
    image: ghcr.io/gke-labs/gemini-for-kubernetes-development/generic-golang:latest
    llm:
      configdirRef: kro-gemini-config
      apiKeySecretRef: gemini-vscode-tokens
      prompt: >-
        You are an expert kubernetes developer who is helping with code reviews.
        This repo you are working on Kube Resource Orchestrator is a kubernetes sig project that aims to simplify the creation and management of complex custom resources for Kubernetes.
        Please look at the most recent commits locally as well as the diff and provide a review feedback.
        Would you approve it ? Please pay attention to the following: 1. Does
        the fix resolve the original problem. 2. Look for linked issues to
        understand the original problem. 3. Are there tests to check the fix.

        If present read @.gemini/user-instructions.json for specific review instructions tuned for this repository.

    maxActiveSandboxes: 1
    maxSandboxes: 5
  issue:
    robotAccount: codebot-robot
    maxActiveSandboxes: 6
    maxSandboxes: 6
    issueShutdownAfterMinutes: 300
    image: ghcr.io/gke-labs/gemini-for-kubernetes-development/generic-golang:latest
    llm:
        apiKeySecretRef: gemini-vscode-tokens
    models:
    - gemini-3-flash-preview
    - gemini-3.1-pro-preview
    - gemini-3.1-flash-lite-preview
    - gemini-2.5-pro
    - gemini-2.5-flash
    - gemini-2.5-flash-lite
    handlers:
    - name: fix
      taskType: fix-issue
      labels:
        - "repo-agent"
      prompt: |
          Fix this issue
---
apiVersion: configdir.gke.io/v1alpha1
kind: ConfigDir
metadata:
  name: kro-gemini-config
spec:
  files:
  - path: .gemini/user-instructions.json
    source:
      inline: |
        ""
