apiVersion: review.gemini.google.com/v1alpha1
kind: RepoWatch
metadata:
  name: rst-gfkd
  namespace: repo-agent-system
spec:
  repoURL: https://github.com/gke-labs/gemini-for-kubernetes-development
  review:
    maxSandboxes: 1
    maxActiveSandboxes: 1
    devcontainerConfigRef: minimal-devcontainer
    maxReviewFiles: 1000
    llm:
      apiKeySecretRef: dummy-provider-token
      provider: dummy
      prompt: "dummy-review-prompt"
  issue:
    maxSandboxes: 0
    maxActiveSandboxes: 0
  dev:
    maxSandboxes: 0
    maxActiveSandboxes: 0
  githubSecretName: github-pat