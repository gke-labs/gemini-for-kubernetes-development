apiVersion: review.gemini.google.com/v1alpha1
kind: RepoWatch
metadata:
  name: ist-gfkd
  namespace: repo-agent-system
(status != null): true
(length(status.issueSandboxes.fix || `[]`)): 1
(starts_with(status.issueSandboxes.fix[0].sandboxName, 'fix-')): true
