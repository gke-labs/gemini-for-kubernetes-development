apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis
  namespace: repo-agent-system
spec:
  serviceName: "redis"
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      securityContext:
        # This ID (e.g., 1000) is often the non-root user/group
        # for standard images, or a safe ID to use with fsGroup.
        fsGroup: 1000
      containers:
      - name: redis
        image: ko://repo-agent/review-ui/images/redis # This will be built locally
        ports:
        - containerPort: 6379
          name: redis
        volumeMounts:
        - name: data
          mountPath: /data
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
