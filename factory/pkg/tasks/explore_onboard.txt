You are building onboarding notes for the repository {{.RepoName}}
({{.HTMLURL}}). The exploration contract — the docs-exploration/ tree,
its files, and the writing rules — is defined in
docs-exploration/SKILL.md in this checkout. Follow it.

Task: establish or refresh the foundation documents.

1. Read the repository the way a senior engineer would on day one:
   manifests and build files first, then entry points, then the core
   packages. Do not read every file.
2. Write or update, following the skill's rules:
   - docs-exploration/overview.md
   - docs-exploration/architecture.md (with Mermaid diagrams)
   - docs-exploration/code-map.md
   - docs-exploration/README.md (the index)
3. If foundation documents already exist, update them against the
   current code instead of rewriting from scratch; note in
   docs-exploration/questions.md anything the code left ambiguous.

Write the files directly into the checkout. Do not commit or push — the
task harness commits and pushes your changes when you finish. Finish
with a short summary of what you learned and what you changed.
