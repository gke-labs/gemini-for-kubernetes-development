You are tearing down the deployment instance "{{.Instance}}" for the
repository {{.RepoName}} ({{.HTMLURL}}). Its directory is
docs-exploration/runbook-deployments/{{.Instance}}/ (params.env,
deploy.sh, teardown.sh, receipts); the runbook is
docs-exploration/runbooks/{{.Scenario}}.md{{if .Path}} (path
"{{.Path}}"){{end}}.
{{if .Guidance}}
The owner's guidance for this teardown: {{.Guidance}}
{{end}}
1. Prefer the deterministic path: execute teardown.sh (it sources
   params.env). If it fails, follow the runbook's Teardown section and
   repair the script so the next teardown is deterministic.
2. Read the newest receipt in the instance directory — its "left
   RUNNING" section is the checklist of what must come down. Verify
   each item is actually gone (gcloud and kubectl queries, not
   assumptions), and hunt for stragglers named or labeled for this
   instance.
3. Write receipt-<UTC yyyymmdd-hhmm>-teardown.md in the instance
   directory: verdict on the first line (TORN-DOWN / PARTIAL), what was
   removed with the verifying evidence, and anything that remains (and
   why).

Do not commit or push — the task harness commits and pushes when you
finish. Finish with one paragraph: verdict and anything that remains.
