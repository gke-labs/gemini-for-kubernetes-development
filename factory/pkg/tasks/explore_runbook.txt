You are writing executable runbooks for the repository {{.RepoName}}
({{.HTMLURL}}). The exploration contract — including the runbook
sections and the requirements rules — is docs-exploration/SKILL.md in
this checkout. Follow it.

{{if eq .Scenario "all"}}Scenarios: the standard set — deploy, upgrade. Write or update
docs-exploration/runbooks/<scenario>.md for each scenario that
genuinely applies to this repository. Where one does not (a library
with no deploy story, a tool with no upgrade path), skip it and record
why in docs-exploration/questions.md instead of padding a file.
{{else}}Scenario: {{.Scenario}} — write or update
docs-exploration/runbooks/{{.Scenario}}.md.
{{end}}
{{if .Guidance}}The owner's guidance: {{.Guidance}}

Treat the guidance as pinned decisions and apply it to the runbook(s)
it concerns — guidance naming a deployment target belongs in the
deploy runbook, not all of them. Write the paths for the targets the
owner named; lift concrete values (project ids, cluster names,
regions) into Preconditions as variables the reader sets. Where the
guidance leaves a choice open, make the call and record the assumption
under the Tier line.
{{end}}
1. Derive each scenario's real path from the repository's own tooling:
   the Makefile, scripts/, CI workflows (.github/workflows is the
   ground truth for what actually builds and tests), helm charts,
   kustomize bases, container files. Do not invent commands — every
   step must trace to something the repo ships.
2. Open What this needs by answering one question plainly: can this
   run in the pod (binaries, tests, envtest, a single-process server),
   or does it need real infrastructure — a cluster or cloud? Node-level
   reality forces the latter: CSI drivers, device plugins, privileged
   DaemonSets, kubelet sockets, host mounts. Say exactly which
   component forces it. If an existing runbook carries (pinned)
   decisions, they are the owner's: keep them, adapt every step to
   them, and note any disagreement in one line under them. Also state
   what credentials or permissions each step assumes.
3. Use the skill's fixed sections — What this needs, Preconditions,
   Steps, Verify, Teardown — with one path per viable target, named by
   the target ("### Path — GKE"). If a runbook already exists,
   update it against the current code instead of rewriting from
   scratch; note steps that changed since the last revision in one
   line at the top.
4. Update docs-exploration/README.md, and add anything the code left
   ambiguous to docs-exploration/questions.md.

Write the files directly into the checkout. Do not commit or push — the
task harness commits and pushes your changes when you finish. Finish
with a short summary of what each scenario needs and where the
runbooks cut corners.
