You are PREPARING a deployment instance for the repository {{.RepoName}}
({{.HTMLURL}}) — writing its scripts, not executing them. The runbook is
docs-exploration/runbooks/{{.Scenario}}.md; the instance is
"{{.Instance}}" and its directory is
docs-exploration/runbook-deployments/{{.Instance}}/.
{{if .Guidance}}
The owner's guidance for this instance: {{.Guidance}}
Treat it as pinned decisions — targets, names, and values it gives are
this instance's parameters.
{{end}}
1. If the instance directory already exists and nothing has drifted —
   the runbook unchanged since the scripts' last commit, the code base
   unchanged in ways that affect them, no new guidance — leave it
   untouched and say so. Otherwise write or update:
   - params.env — every instance parameter as a shell variable
     (PROJECT, REGION, CLUSTER, names…), one place to see what makes
     this instance THIS instance. Defaults from the environment
     (GOOGLE_CLOUD_PROJECT, CLOUDSDK_COMPUTE_REGION) where the
     guidance doesn't pin them. It MUST define
     RESOURCE_PREFIX="${RUNBOOK_RESOURCE_PREFIX}" (already unique
     across every board sharing the project), and every cloud
     resource this instance creates is named
     ${RESOURCE_PREFIX}[-suffix] and labeled
     repo-agent-instance=${RESOURCE_PREFIX} where the service
     supports labels.
   OWNERSHIP: this instance owns everything it touches. Never write
   steps that adopt infrastructure this instance did not create — a
   healthy existing cluster in the project is NOT permission to use
   it; another instance owns it, and your deploy or teardown would
   land on someone else's deployment. Using pre-existing
   infrastructure requires the owner naming it explicitly in the
   guidance above (then pin it in params.env). Empty guidance means
   create your own, never borrow.
   - deploy.sh — deterministic bash, set -euo pipefail, sources
     params.env, every command traceable to the runbook. No secrets,
     ever: credentials come from the environment (Workload Identity).
   - teardown.sh — the runbook's Teardown section, same rules.
2. Do NOT execute anything. Do NOT create cloud resources. This phase
   exists so the scripts are pushed and reviewable before they run.
{{if eq .Mode "plan"}}3. This is a standalone PLAN: nothing executes until the owner
   explicitly deploys. Write the plan receipt to
   docs-exploration/runbook-deployments/{{.Instance}}/receipt-<UTC yyyymmdd-hhmm>.md:
   PLANNED on the first line, then what this instance will create
   (names, from params.env), what it will cost roughly, the
   feasibility state (✓/✗ from the runbook's checklist as it applies
   to this instance), and anything the owner should read in
   deploy.sh before clicking Deploy.
{{end}}

Do not commit or push — the task harness pushes this directory before
the execution phase starts. Finish with one paragraph: the instance's
parameters and anything you had to decide.
